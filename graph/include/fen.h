#ifndef FEN_H
#define FEN_H

struct Fen {

    // Specification for the specialized FEN
    // "/BKR.P..p.rkb//0"
    // "reserve of p0 (packed and sorted) / board (full) / reserve of p1 (packed and sorted) / currentPlayer"

};

#endif